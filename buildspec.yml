version: 0.2
phases:
  install:
    runtime-versions:
      java: latest
  build:
    commands:
      - mvn verify sonar:sonar -Dsonar.projectKey=AWS-Project -Dsonar.organization=Viv3ksGit -Dsonar.host.url=https://sonarcloud.io -Dsonar.token=ec00a659e06f303214ab71424c7ac00592133a1c